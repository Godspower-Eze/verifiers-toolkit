# ── Verifier toolchain paths ──────────────────────────────────────────────────

# Path to the scarb CLI binary (run `which scarb` to find it).
SCARB_PATH=

# Path to the garaga CLI binary (run `which garaga` after `pip install garaga`).
GARAGA_PATH=
